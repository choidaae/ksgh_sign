{
  "name": "training-register-web",
  "version": "1.0.0",
  "description": "연수 등록부 서명·관리 웹앱 (GitHub Pages + Google Apps Script)",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && npx --yes serve dist"
  },
  "engines": { "node": ">=18" }
}
